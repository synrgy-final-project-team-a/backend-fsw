paths:
  /api/user/detail:
    get:  
      summary: get detail user
      security: 
        - BearerAuth: []
      tags: 
        - User Service
      consumes:
        - application/json
      produces:
        - application/json
      responses:
        200:
          description: an array of detail user data
          content:
            "application/json":
              schema:
                allOf:
                  - $ref: "#/components/schemas/JsendSuccess"
                  - properties:
                      data:
                        type: array
                        items:
                          allOf:
                            - properties:
                                id:
                                  type: integer
                            - $ref: "#/components/schemas/Cars"
